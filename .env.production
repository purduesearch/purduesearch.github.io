# Frontend production build env — picked up automatically by `npm run build`.
#
# On CI (GitHub Actions) the REACT_APP_API_URL secret overrides this value,
# so GitHub Pages deployments always use the real Oracle URL automatically.
#
# For a manual local production build, replace the placeholder below:
REACT_APP_API_URL=https://YOUR_ORACLE_SERVER_URL
